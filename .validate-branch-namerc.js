module.exports = {
    pattern: '^(feature|bugfix|hotfix|release)/.+',
    errorMsg:
        '🤨 The branch name is incorrect, rename it with `git branch -m <old-name> <new-name>` following this pattern ^(feature|bugfix|hotfix|release)/.+',
}
